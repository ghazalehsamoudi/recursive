#include<iostream>
#include<conio.h>
using namespace std;
int fibo(int);
main(){
    int a;
	for(int a=1;a<=20;a++)
	    cout<<fibo(a)<<'\t';	
}
int fibo(int i){
	if(i==1 || i==2)
	    return 1;
	return fibo(i-1)+fibo(i-2);
}
